[package]
name = "libasync"
description = "low-level async support"
version = "0.0.0"
authors = ["M-Labs"]
edition = "2018"

[dependencies]
embedded-hal = "0.2"
nb = "1.0"

[dependencies.smoltcp]
version = "0.7"
default-features = false
features = ["alloc", "ethernet", "proto-ipv4", "socket-tcp"]
